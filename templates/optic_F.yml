# Configuration YAML file, type F
# - Spatial filter -

# size (maximum radial coordinate), m
r_max: 0.025

# filter type (ND/MASK/SIN/GAUSS/FREEFORM)
kind: SIN

# if kind == ND: transmittance (0...1)
#T: 0.75

# if kind == MASK: mask radius, m
#r_min: 0.010

# if kind == SIN: distance from center where transmittance starts to decrease, m
# (transmittance becomes 0 at r_max
r_min: 0.010

# if kind == GAUSS: distance from center where transmittance starts to decrease, m
# and half-width of the Gaussian
#r_min: 0
#w: 0.015

# if kind = FREEFORM: tabulated transmittance profile
# format: r T (radial coordinate, m - transmittance)
#form: |
#    0.005 1
#    0.010 0
#    0.020 0.5